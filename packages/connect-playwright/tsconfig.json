{
  "files": ["src/index.ts"],
  "include": ["src/**/*.spec.ts"],
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    // TypeScript 6.0 no longer includes every @types package automatically.
    "types": ["node", "jasmine"]
  }
}
